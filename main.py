"""
MiniDB-Core Entry Point

This is the main entry point for the MiniDB-Core application.
It simply imports and runs the CLI interface.
"""

from src.cli import run


if __name__ == "__main__":
    run()
